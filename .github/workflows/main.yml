name: Mixed inputs with Build

on:
  workflow_dispatch:
    inputs:
      name:
        description: Who to greet
        required: true
  pull_request_target:
    branches:
      - master
  push:
    branches:
      - master

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Send greeting
        run: |
          # Perform some operation
          echo "Hello, ${{ inputs.name }}!"

  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas openpyxl

      - name: Install dependencies
        run: |
          pip install pandas openpyxl

      - name: Check current directory
        run: |
          pwd
          ls

      - name: Read input from Excel file
        run: python main.py --input_file $INPUT_FILE

      - name: Process input data
        run: python main.py --input_file $INPUT_FILE --output_file $OUTPUT_FILE

      - name: Write output to Excel file
        run: python main.py --input_file $INPUT_FILE --output_file $OUTPUT_FILE

      - name: Set output environment variable
        env:
          MY_OUTPUT: ${{ steps.write-output.outputs.output_value }}
        run: echo "MY_OUTPUT=${{ env.MY_OUTPUT }}" >> $GITHUB_ENV

      - name: Set up Python environment
        uses: actions/setup-python@v2
        with:
          python-version: 3.11.2
      - name: Install dependencies
        run: |
          python -m venv myenv
          source myenv/bin/activate
          pip install -r requirements.txt
      - name: Run Python script
        run: |
          source myenv/bin/activate
          python main.py --input_file $INPUT_FILE --output_file $OUTPUT_FILE
          
      - name: Install openpyxl
        run: pip install openpyxl

